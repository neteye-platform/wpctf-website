name: Release on Tag

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: read

jobs:
  build-and-push:
    uses: neteye-platform/repo-commons/.github/workflows/build-docker-image.yaml@cb067dd394dc61458398df6c6ffb903dc0511073 # main
    with:
      working-directory: "."
      dockerfile-path: "./Dockerfile"
      image-name: ${{ github.event.repository.name }}
      push: true
    secrets: inherit
